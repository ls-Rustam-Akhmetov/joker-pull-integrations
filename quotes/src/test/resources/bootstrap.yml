spring:
  application:
    name: quotes
  cloud:
    config:
      enabled: true

  datasource:
    url: jdbc:sqlserver://localhost:1433
    hikari:
      maximum-pool-size: 1
      connection-timeout: 1000

historical-quote:
  table:
    name: DWH_PERSBROKER.DWH.vHistorical_Quotes

quik.source.name: QUIK
bloomberg.source.name: BLOOMBERG-ADAPTER

eureka:
  client:
    enabled: false

